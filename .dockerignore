# Allowlist: only what the image needs. Keeps data, weights, caches and
# local files out of the build context.
*
!pyproject.toml
!uv.lock
!README.md
!src/
!configs/
!scripts/
!tests/
!third_party/
**/__pycache__
